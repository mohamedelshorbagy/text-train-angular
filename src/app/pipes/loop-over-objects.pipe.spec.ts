import { LoopOverObjectsPipe } from './loop-over-objects.pipe';

describe('LoopOverObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new LoopOverObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
